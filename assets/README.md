# Asset slots for scripts/build_ad.py

Drop these in and re-run `python scripts/build_ad.py`. Nothing else changes.

| File | What it is |
|---|---|
| `cgf_logo.png` | CGF logo, transparent PNG, roughly 4:1 landscape |
| `poultry_roof.jpg` | Poultry house with a roof array. Landscape, wide crop, ideally 3:1 or wider. A real NEA Solar install beats stock. |

Also edit at the top of `scripts/build_ad.py`:

- `PHONE` — appears twice, in the masthead and the call bar
- `BRAND` / `BRAND_DK` — currently a barn red placeholder, swap for real brand colors
